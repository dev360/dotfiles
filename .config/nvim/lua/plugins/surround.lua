return {
  "kylechui/nvim-surround",
  event = "VeryLazy",
  config = function()
    require("nvim-surround").setup({
      -- Add any custom configuration here, or leave empty for the defaults
    })
  end,
}
